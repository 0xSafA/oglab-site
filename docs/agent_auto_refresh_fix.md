# Исправление проблемы с автообновлением страницы при работе с агентом

## Проблема

Пользователи не могли вводить диалог с AI-агентом на странице меню, потому что страница "моргала" каждые ~10 секунд. Причина — компонент `AutoRefresh` с watchdog-механизмом, который ложно определял "зависание" страницы во время взаимодействия с агентом и пытался перезагрузить страницу.

### Что вызывало проблему

1. **Watchdog механизм** в `AutoRefresh.tsx` проверял "зависание" главного потока каждые 10 секунд
2. При вводе текста в агента или отправке запросов главный поток мог тормозиться
3. Watchdog интерпретировал это как зависание и пытался перезагрузить страницу
4. Защита от частых перезагрузок блокировала reload (минимум 5 сек между попытками)
5. Пользователь видел "моргание" и не мог нормально работать с агентом

## Решение

### 1. Улучшена логика `AutoRefresh` (src/components/AutoRefresh.tsx)

**Добавлена проверка активности агента:**
```typescript
const isAgentActive = () => {
  if (typeof document === 'undefined') return false;
  
  // Проверяем, открыт ли агент по различным признакам
  const agentContainer = document.querySelector('[data-agent-active="true"]');
  const agentInput = document.querySelector('[data-agent-input]');
  const hasActiveInput = document.activeElement?.getAttribute('data-agent-input') === 'true';
  
  return !!(agentContainer || agentInput || hasActiveInput);
};
```

**Обновлена функция `safeReload`:**
- Добавлена проверка активности агента перед перезагрузкой
- Если агент активен, перезагрузка откладывается на 30 секунд
- Добавлено логирование для отладки

**Улучшен Watchdog:**
- **КРИТИЧНО**: Если агент активен, watchdog НЕ проверяет зависания
- Добавлена проверка фокуса на INPUT/TEXTAREA элементах
- Улучшено логирование с указанием причин сброса счетчика зависаний

### 2. Обновлен компонент агента (src/components/OGLabAgent.tsx)

**Добавлены data-атрибуты для идентификации:**
```tsx
<section 
  ref={agentContainerRef}
  data-agent-active="true"  // ← Маркер активного агента
  className={...}
>

<input
  data-agent-input="true"  // ← Маркер поля ввода агента
  ...
/>
```

Эти атрибуты позволяют `AutoRefresh` определить, что пользователь взаимодействует с агентом.

## Результат

✅ **Страница больше не "моргает" при работе с агентом**
✅ **Пользователи могут нормально вводить текст и получать ответы**
✅ **Watchdog по-прежнему работает для защиты от реальных зависаний**
✅ **Автообновление продолжает работать для синхронизации с базой данных**

## Как это работает

1. Пользователь открывает страницу меню с агентом
2. При клике на поле ввода агента, `document.activeElement` становится input с `data-agent-input="true"`
3. Watchdog проверяет `isAgentActive()` каждые 10 секунд
4. Если агент активен, watchdog пропускает проверку зависаний
5. Если приходит команда перезагрузки (Supabase realtime), `safeReload` проверяет агента
6. Перезагрузка откладывается, если пользователь печатает
7. Через 30 секунд проверка повторяется

## Дополнительные улучшения

- **Защита от частых перезагрузок**: минимум 5 секунд между попытками
- **Проверка активных анимаций**: если есть анимации, значит страница работает
- **Проверка фокуса на input/textarea**: защита для всех форм, не только агента
- **Улучшенное логирование**: теперь видно причину каждой попытки перезагрузки

## Мониторинг

Откройте консоль браузера (F12 → Console) чтобы видеть:
- `🐕 Watchdog: ...` — параметры watchdog при загрузке
- `⚠️ Watchdog: main thread stall detected` — обнаружено зависание
- `🚫 RELOAD BLOCKED: Too frequent reload attempt` — заблокирована частая перезагрузка
- `🚫 RELOAD DELAYED: Agent is active` — перезагрузка отложена из-за активного агента
- `✅ Watchdog: lag recovered` — зависание устранено
- `🔄 RELOAD EXECUTING` — страница перезагружается

## Дата исправления

5 октября 2025 г.
